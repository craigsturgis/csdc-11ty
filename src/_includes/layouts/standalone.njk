---
layout: base.njk
---

<article class="max-w-prose mx-auto px-4 sm:px-6 py-8">
    <header class="mb-8">
        <p class="eyebrow mb-4">Get in touch</p>
        <h1 class="text-5xl sm:text-7xl font-semibold tracking-[-0.05em] mb-8">{{ title }}</h1>
    </header>

    <div class="prose prose-lg prose-invert max-w-none">
        {{ content | safe }}
    </div>
</article>
